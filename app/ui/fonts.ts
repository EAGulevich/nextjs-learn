import {Megrim, Inter, Lusitana} from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const megrim = Megrim({ subsets: ['latin'], weight: '400'});
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700']});
